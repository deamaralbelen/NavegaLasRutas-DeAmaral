const Container = ({ mensaje }) => {
    return (
        <div>
            <h2>{mensaje}</h2>
            {/* Acá van a ir los productos en el futuro */}
        </div>
    );
};

export default Container;